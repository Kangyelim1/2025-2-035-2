using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum BlockType
{
    // InventoryManager의 Item Sprites 배열 순서와 일치하도록 0부터 순차적으로 정의하세요.
    Dirt = 0,
    Grass = 1,
    Water = 2,
    Tree = 3,
    rock = 4,
    ore = 5,
    // ... 필요한 블록 타입 추가
}
